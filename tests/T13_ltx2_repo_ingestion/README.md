# T13 — LTX-2 repository ingestion

Runs the current static inspector against the separately supplied Appvikalabs source ZIP/directory. Raw source workflows are deliberately not bundled.

`python run_test.py <source-zip-or-directory>`
