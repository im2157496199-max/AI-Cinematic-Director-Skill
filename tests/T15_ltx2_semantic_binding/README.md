# T15 — semantic Set/Get binding

Build-time test uses the separately supplied real `LTX-2 - I2V Basic.json` UI workflow to prove semantic anchor tracing. The packaged API fixture is synthetic and exists only to test conservative UI-node→API-node pairing; it is **not** claimed to be an export of the community workflow.

`python run_test.py <path-to-real-I2V-Basic.json>`
