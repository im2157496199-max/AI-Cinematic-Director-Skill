# T11 — API Format Contract Regression
Tests the API graph contract without pretending to run ComfyUI:
- API-format recognition;
- allowlisted scalar patching;
- typed constraints;
- topology/class signature invariant;
- refusal to overwrite a linked input.
