# T16 Final Format

Validates the unpacked release root against `FINAL_FORMAT_CONTRACT.md`.

Checks:
- versionless ZIP root naming convention;
- `VERSION` and `SKILL.md` frontmatter agreement;
- required final-format files/directories;
- no historical Release Notes / Migration / old Build Audit clutter;
- no engineering-version suffixes in current knowledge/document filenames.
