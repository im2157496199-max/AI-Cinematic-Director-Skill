# T12 Test Report
PASS.

Verified against a deterministic frozen mock `/object_info` snapshot:
- valid classes/inputs pass;
- missing node class is detected before submission.

This is a regression test for the preflight logic, not evidence of the user's current live node registry.
